# Plan: signal-loop

1. Add canonical local schemas and atomic writes for signals, Opinion Dock rules,
   triage, and Product Owner decisions.
2. Add deterministic explainable scoring, hands-off enforcement, abstract model
   routing profiles, and a no-connector authority boundary.
3. Promote approved complete signals through the existing Product Graph compiler;
   emit a needs-input draft for incomplete product contracts.
4. Extend missions with creator-verifier role separation, context-wall inputs,
   and exact completion criteria.
5. Add independent completion and drift verification receipts.
6. Expose CLI commands, docs, Mermaid architecture, tests, smoke checks, and
   release metadata.
7. Run SpecLine mutation, ForgeLine QA/smoke/challenge, package builds, editor
   tests, clean-wheel smoke, then publish a reviewable GitHub PR.
