# Changelog

## 0.2.0

- Add the loopback-only Factory Studio command with explicit workspace trust.
- Accept only literal `127.0.0.1` startup URLs and open them through the VS Code API.
- Terminate the local Studio child process when the extension is disposed.

## 0.1.1

- Add a read-only local meter overview with explicit workspace confirmation.

## 0.1.0

- Run FactoryLine assembly and feature verification from VS Code.
- Render local JSON receipts in an adjacent editor panel.
